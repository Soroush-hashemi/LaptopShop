@using Common.Application.FileUtil
@using WebApplication.Areas.Admin.Models.SliderPosters
@model List<SliderPosterViewModel>
@{
    ViewData["title"] = "اسلایدر_پوستر";
    ViewData["selected"] = "اسلایدر_پوستر";
    var counter = 1;
}
<section class="content-header">
    <h1>
        @ViewData["title"]
    </h1>
    <ol class="breadcrumb">
        <li><a href="~/Admin"><i class="fa fa-dashboard"></i> خانه</a></li>
        <li class="active">@ViewData["title"]</li>
    </ol>
</section>
<section class="content">
    <a class="btn btn-success" style="margin-bottom: 5px" asp-area="Admin" asp-controller="SliderPoster" asp-action="Add">افزودن اسلایدر_پوستر جدید</a>
    <div class="box box-primary ">
        <div class="box-body p-0">
            <table class="table table-bordered table-hover">
                <thead>
                    <tr>
                        <th>#</th>
                        <th>عکس</th>
                        <th>محل قرارگیری</th>
                        <th>عملیات</th>
                    </tr>
                </thead>
                <tbody>
                    @if (Model != null)
                    {
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@counter</td>
                                <td>
                                    <img style="max-width: 150px" src="@Directories.GetSliderPosterImage(item.Image)" />
                                </td>
                                <td>
                                    @item.ImageLocationDto
                                </td>
                                <td>
                                    <a asp-area="admin" asp-controller="SliderPosters" asp-action="Edit" asp-route-Id="@item.Id" class="btn btn-info btn-sm">ویرایش</a>
                                    <a asp-area="admin" asp-controller="SliderPosters" asp-action="Delete" asp-route-Id="@item.Id" class="btn btn-danger btn-sm">حذف</a>
                                </td>
                            </tr>
                            counter += 1;
                        }
                    }
                </tbody>
            </table>
        </div>
    </div>
</section>